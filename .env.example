# Copiar a .env.local (NUNCA se commitea — ya cubierto por .gitignore vía *.local) y completar
# con los valores reales desde el dashboard de Supabase: Project Settings → API Keys.

VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxx
